name: tp_8
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Dépendances blockchain + state management
  provider: ^6.1.5+1
  web3dart: ^2.7.1
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  crypto: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - src/artifacts/HelloWorld.json
